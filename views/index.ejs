<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>

</head>
<body>
    
       <div class="h-screen w-screen bg-black p-10 text-white text-3xl">
        <div>
            <form action="/create" method="post">

                <input class="w-full rounded-md  border-2 border-gray-300 my-5 " placeholder="Enter the title" name="title" type="text">
                <textarea class="w-full rounded-md border-2 border-gray-300 my-5" placeholder="enter the message" name="details" id=""></textarea>
                <input class="bg-blue-600 px-5 py-3 rounded-md " type="submit" value="Create">
            </form>
        </div>
               
        <div class="h-auto flex flex-wrap gap-7 mt-5">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){%>
                         <div class="max-w-80 bg-gray-400 px-3 py-2 rounded-md">
                            <h1><%= val %></h1>
                            <a class="text-2xl text-blue-600" href="/file/<%=val%>">read more</a>
                        </div>    
               <% }) %>
           <% } else{%>
                   <h1>tha data has not yet</h1>
           <%} %>
            

        </div>  
        
        
        </div>
        
       
    
</body>
</html>